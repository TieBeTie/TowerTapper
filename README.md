Необходимо реализовать     [SkillType.MULTISHOT];, начиная от кнопки (уже реализована), переходя к UIManager и т д., берётся с помощью SkillSetManager, он будет работать так, есть небольшой шанс того, что будет выпущено сразу 3 стрелы в сторону врага с небольшим отклонением по градусам (в центр, чуть левее и чуть правее). Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, чтобы сделать это правильно и красиво.

Необходимо реализовать     [SkillType.CRITICAL_CHANCE, SkillType.CRITICAL_MULTIPLIER];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 0.25 и 0.25 с повышением на 0.25) берутся с помощью SkillSetManager, он будет работать так, чтобы критический удар наносился с небольшим шансом, и урон был увеличенным. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, чтобы сделать это правильно и красиво.

Необходимо реализовать     [SkillType.Lifesteal chance, SkillType.Lifesteal Amount];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 0.25 и 0 с повышением на 0.25 и 1 соответственно) берутся с помощью SkillSetManager, будет работать так, при попадании стрелы во врага, есть шанс SkillType.Lifesteal  своровать здоровье в количестве SkillType.Lifesteal Amount. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, чтобы сделать это правильно и красиво.



Необходимо реализовать     [SkillType.DailyGoldBounus] ;, начиная от кнопки (уже реализована), переходя к UIManager и т д., значения берутся с помощью SkillSetManager, золото перечисляется в начале каждой волны, очень простая логика, если он купил daily gold bonus 1, ему будет перечисляться 2, если 2, то 3, и так далее. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, сделать это правильно и красиво.


Необходимо реализовать     [SkillType.Free Upgrade Chance];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 0, с повышением на 0.05) берутся с помощью SkillSetManager, будет работать так, при нажатии на любую кнопку апгрейда, есть шанс SkillType.Free Upgrade Chance, чтобы он был бесплатным. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, чтобы сделать это правильно и красиво.


Необходимо реализовать     [SkillType.Supply Drop Chance];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 0, с повышением на 0.05) берутся с помощью SkillSetManager, будет работать так, при нажатии на любую кнопку апгрейда, есть шанс SkillType.Supply Drop Chance, что упадёт коробка с золото в рандомном месте на карте, которую надо подобрать тапом. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, чтобы сделать это правильно и красиво.

Необходимо реализовать     [SkillType.Max Speed];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 1, с повышением на 0.25) берутся с помощью SkillыSetManager, будет работать так, при нажатии на любую кнопку апгрейда, ВСЕ действия в игре будут происходить в на со скоростью SkillSetManager.get... Он нужен, чтобы игрок не скучал от монотонности игры. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, никаких констант с неба, сделать это правильно и красиво.


Необходимо реализовать     [SkillType.Max Speed];, начиная от кнопки (уже реализована), переходя к UIManager и т д., коэффициенты (базовый 0, с повышением на 0.05) берутся с помощью SkillSetManager, будет работать так, при нажатии на любую кнопку апгрейда, есть шанс SkillType.Max Speed, что скорость врагов будет увеличена. Обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, чтобы сделать это правильно и красиво.




Перед выполнением следующего задания обязательно нужно просмотеть всю архитектуру и понять как она работает, чтобы реализовать этот функционал, сохранить гибкость и несвязность компонентов, сделать это правильно и красиво.ф



Пришло время добавить в игру новый тип валюты heraldic_emblem, они будут даваться с каждой волной по одной штуке (базовое значение в SkillSetManager), их можно будет тратить на покупку постоянных улучшений, сейчас же можно покупать улучшения только в игре, затем они обнуляются до базовых значений. 

Задача реализовать что-то что будет хранить эмблемы какой-нибудь storage, что-то чем можно будет управлять ими, затем реализовать кнопку улучшений [SkillType.Gem Bonus (надо переименовать в Emblem Bonus)];, начиная от кнопки (уже реализована), переходя к UIManager и т д., (базовый 1, с повышением на 1), getEmblemBonus() будет брать значение из SkillSetManager, и будет прибавляться каждую волну, как это было сделано с золотом.



